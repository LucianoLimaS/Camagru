RewriteEngine On

# Impede listagem de diretórios
Options -Indexes

# Redireciona tudo para o index.php se o arquivo ou diretório físico não existir
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ index.php [QSA,L]
